# Conexión a la base de datos 
spring.datasource.url = jdbc:mysql://localhost:3306/sistema_examenes_springboot?useSSL=false&serverTimezone=UTC

# Usuario de la base de datos
spring.datasource.username = root

# Contrasela de la base de datos
spring.datasource.password = 35912768Ruth

#Propiedad para usar el dialecto de mysql
spring.jpa.properties.hibernate.dialect = org.hibernate.MySQLDialect

#Propiedad para actualizar las tablas de la base de datos sin que se creen nuevamente
spring.jpa.hibernate.ddl-auto = update

#Muestra las sentencias sql cuando se inserta un registro
spring.jpa.show-sql = true

#Muestra las sentencias en el formato sql
spring.jpa.properties.hibernate.format_sql = true
